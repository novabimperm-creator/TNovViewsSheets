<Window x:Class="TNovViewsSheets.SheetNumWPF"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="620"
        Height="560"
        MinWidth="520"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        WindowStyle="None">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TNovCommon;component/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="12"
            Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="1">
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent" Cursor="Arrow" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="35"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="НУМЕРАТОР ЛИСТОВ"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="✕" Grid.Column="1" Style="{StaticResource CloseButtonStyleMini}"
                            Click="escButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Комплект:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Width="70" Margin="0,0,8,0"/>
                <ComboBox x:Name="TypeComboBox" Width="160" Style="{StaticResource DarkComboBoxStyle}" SelectionChanged="TypeComboBox_SelectionChanged"/>
                <Button x:Name="ApplyFilterButton" Content="Отобразить" Style="{StaticResource SecondaryButtonStyle}"
                        Padding="10,6" Margin="8,0,0,0" Click="ApplyFilterButton_Click"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Начальный номер:" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Width="120" Margin="0,0,8,0"/>
                <TextBox x:Name="StartOrderTextBox" Width="100" Height="36" Style="{StaticResource DarkTextBoxStyle}" Text="0"/>
                <Button x:Name="RenumberButton" Content="Пересчитать" Style="{StaticResource SecondaryButtonStyle}"
                        Padding="10,6" Margin="8,0,0,0" Click="RenumberButton_Click"/>
            </StackPanel>

            <TextBlock x:Name="CurrentFilterText" Grid.Row="3" Foreground="{StaticResource AccentBrush}"
                       FontWeight="SemiBold" Visibility="Hidden" Margin="0,0,0,8"/>

            <ListView x:Name="ItemsListView" Grid.Row="4" AllowDrop="True"
                      PreviewMouseLeftButtonDown="ListView_PreviewMouseLeftButtonDown"
                      Drop="ListView_Drop" DragOver="ListView_DragOver"
                      Background="Transparent" BorderThickness="0" Foreground="White" Margin="0,0,0,12">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Номер" Width="60" DisplayMemberBinding="{Binding TNovSheetNumericNumber}"/>
                        <GridViewColumn Header="Имя листа" Width="420">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TNovSheetName}" Foreground="White" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="VerticalContentAlignment" Value="Top"/>
                        <Setter Property="Height" Value="Auto"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Style="{StaticResource CloseButtonStyle}" x:Name="escButton"
                        MinWidth="100" Margin="0,0,8,0" Click="escButton_Click"/>
                <Button Content="Применить" Style="{StaticResource RunButtonStyle}" IsDefault="True" Margin="0"
                        MinWidth="100" Click="acceptButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
