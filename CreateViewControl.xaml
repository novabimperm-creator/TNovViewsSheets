<UserControl x:Class="TNovViewsSheets.CreateViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <!-- Стиль с полным шаблоном -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Убираем SystemColors -->
                        <ControlTemplate.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" 
                                 Color="Transparent"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                 Color="Transparent"/>
                        </ControlTemplate.Resources>

                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Прямое указание цвета -->
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Цвет заголовка -->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Темнее -->
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" 
                            Property="Opacity" 
                            Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="Выберите тип вида:" Margin="0,5"/>
        <ComboBox x:Name="CbViewFamilyTypes" Margin="0,5" SelectionChanged="CbViewFamilyTypes_SelectionChanged"/>

        <!-- Панель для чертёжных видов (Drafting) -->
        <StackPanel x:Name="DraftingPanel" Margin="0,5" Visibility="Visible">
            <TextBlock Text="Шаблон имени (используйте {#} для номера):" Margin="0,5"/>
            <TextBox x:Name="TbDraftingNameTemplate" Margin="0,5" Text="Р_01_Этаж{#}"/>
        </StackPanel>

        <!-- Панель для планов этажей (Floor Plans) -->
        <StackPanel x:Name="FloorPlanPanel" Margin="0,5" Visibility="Collapsed">
            <TextBlock Text="Выберите уровень:" Margin="0,5"/>
            <ComboBox x:Name="CbLevels" Margin="0,5"/>

            <TextBlock Text="Шаблон имени (используйте {#} для номера и {Level} для имени уровня):" Margin="0,10,0,5"/>
            <TextBox x:Name="TbFloorPlanNameTemplate" Margin="0,5" Text="План {Level} {#}"/>

            <TextBlock Text="Видовой шаблон (необязательно):" Margin="0,10,0,5"/>
            <ComboBox x:Name="CbViewTemplates" Margin="0,5"/>
        </StackPanel>

        <!-- Общее поле количества -->
        <TextBlock Text="Количество видов:" Margin="0,10,0,5"/>
        <TextBox x:Name="TbViewCount" Margin="0,5" Text="1"/>

        <Button Content="Создать вид(ы)" Margin="0,15" Padding="5" Click="CreateViews_Click" Style="{StaticResource CustomButtonStyle}"/>
    </StackPanel>
</UserControl>