<UserControl x:Class="TNovViewsSheets.ExportPdfControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <!-- Стиль с полным шаблоном -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Убираем SystemColors -->
                        <ControlTemplate.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" 
                                 Color="Transparent"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                 Color="Transparent"/>
                        </ControlTemplate.Resources>

                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Прямое указание цвета -->
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Цвет заголовка -->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Темнее -->
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" 
                            Property="Opacity" 
                            Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="5">
            <GroupBox Header="Опции экспорта" Padding="5">
                <StackPanel>
                    <!-- Папка -->
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Папка экспорта:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="TbOutputFolder" Width="300" Margin="5,0"/>
                        <Button Content="Выбрать" Width="60" Click="BrowseFolder_Click" Style="{StaticResource CustomButtonStyle}"/>
                    </StackPanel>

                    <!-- Формат экспорта -->
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Формат экспорта:" VerticalAlignment="Center"/>
                        <RadioButton x:Name="RbFormatPdf" Content="PDF" GroupName="ExportFormat" IsChecked="True" Margin="5,0,0,0" Checked="Format_Checked"/>
                        <RadioButton x:Name="RbFormatDwg" Content="DWG" GroupName="ExportFormat" Margin="5,0,0,0" Checked="Format_Checked"/>
                    </StackPanel>

                    <!-- Объединение (только для PDF) -->
                    <CheckBox x:Name="ChkCombine" Content="Объединить все выбранные в один PDF-файл" Margin="0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5" x:Name="CombineFileNamePanel">
                        <TextBlock Text="Имя итогового файла:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="TbCombineFileName" Width="200" Text="Имя файла"/>
                        <TextBlock Text=".pdf" VerticalAlignment="Center" Margin="5,0"/>
                    </StackPanel>
                    <!-- Дополнительные параметры (только для PDF) -->
                    <Expander x:Name="ExpanderOptions" Header="Дополнительные параметры" Margin="0,5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="Масштаб:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <ComboBox x:Name="CbScale" Width="100" SelectedIndex="0">
                                    <ComboBoxItem Content="По размеру страницы"/>
                                    <ComboBoxItem Content="100%"/>
                                    <ComboBoxItem Content="50%"/>
                                    <ComboBoxItem Content="25%"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="Ориентация:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <RadioButton x:Name="RbPortrait" Content="Книжная" GroupName="Orientation" IsChecked="True"/>
                                <RadioButton x:Name="RbLandscape" Content="Альбомная" GroupName="Orientation" Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Expander>

                    <!-- Кнопка экспорта -->
                    <Button Content="Экспорт" Width="100" Margin="0,15" HorizontalAlignment="Center" Click="Export_Click" Style="{StaticResource CustomButtonStyle}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>