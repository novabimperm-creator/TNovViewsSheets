<UserControl x:Class="TNovViewsSheets.CreateModeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <!-- Стиль с полным шаблоном -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Убираем SystemColors -->
                        <ControlTemplate.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" 
                                 Color="Transparent"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                 Color="Transparent"/>
                        </ControlTemplate.Resources>

                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Прямое указание цвета -->
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Цвет заголовка -->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" 
                            Property="Background" 
                            Value="#FF008E43"/>
                                <!-- Темнее -->
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" 
                            Property="Opacity" 
                            Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="Семейство штампа (тип):" Margin="0,5"/>
        <ComboBox x:Name="CbTitleBlocks" Margin="0,5" 
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="Symbol"/>
        <TextBlock Text="Формат и ориентация:" Margin="0,10,0,5"/>
        <StackPanel Orientation="Horizontal">
            <ComboBox x:Name="CbFormat" Margin="0,5" IsEditable="True" Width="80" DisplayMemberPath="DisplayName" SelectedValuePath="Format"/>
            <ComboBox x:Name="CbOrientation" Margin="0,5" IsEditable="True" Width="110" DisplayMemberPath="DisplayName" SelectedValuePath="VerticalOrientation"/>
        </StackPanel>
        
        <TextBlock Text="Комплект (A_Комплект чертежей):" Margin="0,10,0,5"/>
        <ComboBox x:Name="CbSheetSets" Margin="0,5" IsEditable="True"/>
        
            <TextBlock Text="Количество листов:" Margin="0,10,0,5"/>
        <TextBox x:Name="TbSheetCount" Margin="0,5" Text="1"/>

        <Button Content="Создать лист(ы)" Margin="0,15" Padding="5" Click="CreateSheets_Click" Style="{StaticResource CustomButtonStyle}"/>
    </StackPanel>
</UserControl>